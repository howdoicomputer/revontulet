# About

The `/deploy/k8s/revontulet-argocd` directory contains Kubernetes manifests that use GKE and ArgoCD APIs to enable a gitops deployment model against an ArgoCD enabled cluster.

Not currently used as I've used up most of my free credits from GCP.

---
